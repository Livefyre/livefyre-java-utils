test:
  override:
  - mvn clean install
  - mvn test -Dtest=IntegrationTestSuite
  - sudo update-alternatives --set java oraclejdk7
  - mvn clean install
  - mvn test -Dtest=IntegrationTestSuite
  - sudo update-alternatives --set java oraclejdk8
  - mvn clean install
  - mvn test -Dtest=IntegrationTestSuite

machine:
  timezone:
    America/Los_Angeles
  java:
    version: oraclejdk6